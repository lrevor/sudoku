// TODO: Determine if a block has a value in only one row/col, if so clear from other blocks in row/col
extension sudokuData {
    func scanBlockSingle() -> Bool {
        var found = false
        return found
    }
}