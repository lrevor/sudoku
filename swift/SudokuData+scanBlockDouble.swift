// TODO: Determine if a value appears in 2 columns/rows in 2 aligned blocks, if so, clear it from the 3rd block for those rows/cols

extension sudokuData {
    func scanBlockDouble() -> Bool {
        var found = false
        return found
    }
}